import { Button } from "react-native-paper";
const EditButton = () => {
    return (
        <Button 
            mode="contained" 
            buttonColor="lightgray" 
            textColor="black"
        >
            Edit
        </Button>
    );
};

export default EditButton;